import React from 'react';
import { ParameterTypeCell } from './parameter-type-cell';
import { withTranslation } from 'react-i18next';

export default React.memo(withTranslation('common')(ParameterTypeCell));
