import withStyles from '@material-ui/core/styles/withStyles';
import { styles } from './styles';
import { RealConstraint } from './real-constraint';

export default withStyles(styles)(RealConstraint);
